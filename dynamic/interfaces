#/etc/network/interfaces 
#cp ./dynamic/interfaces /etc/network/interfaces 
auto lo
iface lo inet loopback 
auto eth0
iface eth0 inet dhcp
#ifdown eth0 && cp ./dynamic/interfaces /etc/network/interfaces  && ifup eth0
 
